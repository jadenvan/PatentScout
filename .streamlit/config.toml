[server]
# Increase max upload size for design sketch images (in MB)
maxUploadSize = 10

[browser]
# Disable telemetry
gatherUsageStats = false

[theme]
primaryColor = "#3498DB"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#2C3E50"
font = "sans serif"
